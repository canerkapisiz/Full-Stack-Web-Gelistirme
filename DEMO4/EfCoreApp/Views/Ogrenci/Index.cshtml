@model IEnumerable<Ogrenci>

<div class="row container">
    <div class="d-flex justify-content-between align-items-center mb-5">
        <a asp-controller="Ogrenci" asp-action="Create" class="btn btn-primary">Öğrenci Ekle</a>
        <hr>
        <div class="col-md-6 mb-3">
            <form asp-action="Index" asp-controller="Ogrenci" method="get">
                <div class="input-group">
                    <input type="text" class="form-control" placeholder="Öğrenci adı ile ara" name="searchString">
                    <div class="input-group-append">
                        <button type="submit" class="btn btn-secondary">Ara</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
    @foreach (var ogrenci in Model)
    {
        <div class="col-md-3 mb-3">
            <div class="card" style="width: 18rem;">
                <img src="~/img/@ogrenci.Image" alt="" class="card-img top img-fluid"
                    style="max-height: 200px; object-fit: cover;">
            </div>
            <div class="card-body">
                <h5 class="card-title">@ogrenci.AdSoyad</h5>
                <p class="card-text">@ogrenci.Eposta</p>
                <a asp-controller="Ogrenci" asp-action="Profile" asp-route-id="@ogrenci.OgrenciId"
                    class="btn btn-primary">Detay</a>
            </div>
        </div>
    }
</div>
